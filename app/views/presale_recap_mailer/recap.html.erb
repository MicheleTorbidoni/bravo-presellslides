<%# The operator-edited plain-text body, with line breaks preserved. Bare URLs (the
    video deep-dive links) are left as-is — mail clients linkify them automatically. %>
<div style="font-family: Arial, Helvetica, sans-serif; font-size: 14px; line-height: 1.5; color: #1f2430;">
  <%= simple_format(@body) %>
</div>
